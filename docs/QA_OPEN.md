# 验收：点图标打开

只保留 **一个** 应用：`/Applications/Loaflings.app`（不再往桌面复制）。

```bash
cd ~/Library/Mobile\ Documents/com~apple~CloudDocs/Loaflings
git pull
npm run install:qa
```

Dock：打开一次 → 右键 → 选项 → 在 Dock 中保留。

辅助功能勾 **Loaflings**。HUD 显示合计 **hits**（打字+点击）。设置里可切中/英、藏按键。背包里有日历可看哪天孵化了什么。
